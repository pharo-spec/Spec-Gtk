Extension { #name : #GtkBaseStore }

{ #category : #'*Spec-Gtk' }
GtkBaseStore >> roots: aCollection [
	"Compatibility with GtkTreeDataStore, it should just be used when removing... hence 
	 not really needed."
]
